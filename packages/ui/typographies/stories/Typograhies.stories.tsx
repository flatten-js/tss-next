import type { Meta, StoryObj } from '@storybook/react'
import { Typographies } from '../src'

const meta: Meta<Typographies> = {
  title: 'Typographies',
  component: Typographies
}

type Story = StoryObj<Typographies>

export const Default: Story = {}

export default meta